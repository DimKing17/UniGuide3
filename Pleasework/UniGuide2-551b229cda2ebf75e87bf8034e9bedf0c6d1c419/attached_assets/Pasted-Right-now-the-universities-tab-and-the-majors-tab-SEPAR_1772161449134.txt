Right now, the “universities” tab and the “majors” tab SEPARATELY. However, that’s not good since they go hand in hand. 

Update the app so the top nav includes a single “Selections” entry point instead of both the “universities and “majors” tab. When a student clicks Selections, open a single workspace where: Right side = Majors picker. Left side = Universities picker.


If the student clicks Majors, show the existing Majors UI exactly as it currently works (search, categories, the optional quiz for “I don’t know”, all filters and content). After a major is selected (example: Mechanical Engineering), show a short microflow: let the student either select additional majors (multi-select up to 5) or return to Selections to pick universities. The selected major(s) must be kept in memory while switching tabs.


If the student switches to Universities (from the Selections workspace), show the current Universities UI (search, filters, program list) but add two things:


Each program card must include a hidden expandable dropdown (collapsed by default) that contains program info (Degree, Co-op flag, Target Avg, Admission Date, Tuition/yr, Competitiveness, and required high-school courses). Don’t show these details by default—only when the student expands the dropdown. 


Add a simple, explainable rank/score for each university card showing how well that university/program fits the student’s currently selected major(s). The rank must be derived from obvious signals (course prerequisites matched, co-op preference, distance filter) and must show 1–2 short reason badges (e.g., “requires Calculus”, “co-op match”). Keep the scoring transparent and simple (do not invent complex ML). 


For both the majors and universities parts, there should be several filters.


When the student selects a university program (example: University of Toronto — Mechanical Engineering), automatically add a single item to My List with the label formatted: University Name — Major (e.g., University of Toronto — Mechanical Engineering). My List must update live and be reachable from its existing tab. Saved items should also store which major(s) the user had selected at save time.


Keep all previous Majors features (quiz, saved majors, suggested ECs, etc.) exactly as they worked before — do not remove functionality. Do not import or change bulk datasets in this job.


Minor UX polish: show a small toast/confirmation after saving (e.g., “Saved to My List”), and allow a single-click undo in that toast.
